//const canvas = document.getElementById("Matrix"),
var canvas = document.getElementById("Matrix"),
    context = canvas.getContext("2d"),
    katakana = "アァカサタナハマヤャラワガザダバパイィキシチニヒミリヰギジヂビピウゥクスツヌフムユュルグズブヅプエェケセテネヘメレヱゲゼデベペオォコソトノホモヨョロヲゴゾドボポヴッン",
    latin = "ABCDEFGHIJKLMNOPQRSTUVWXYZ",
    nums = "0123456789",
    alphabet = "アァカサタナハマヤャラワガザダバパイィキシチニヒミリヰギジヂビピウゥクスツヌフムユュルグズブヅプエェケセテネヘメレヱゲゼデベペオォコソトノホモヨョロヲゴゾドボポヴッンABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789",
    fontSize = 16,
    columns = canvas.width;
(canvas.width = window.innerWidth), (canvas.height = window.innerHeight);
var rainDrops = [];
for (let x = 0; x < columns; x++) rainDrops[x] = 1;
var draw = () => {
    (context.fillStyle = "rgba(0,0,0,0.05)"), context.fillRect(0, 0, canvas.width, canvas.height), (context.fillStyle = "#0F0"), (context.font = "16px monospace");
    for (let t = 0; t < rainDrops.length; t++) {
        let a = alphabet.charAt(Math.floor(Math.random() * alphabet.length));
        context.fillText(a, 16 * t, 16 * rainDrops[t]), 16 * rainDrops[t] > canvas.height && Math.random() > 0.975 && (rainDrops[t] = 0), rainDrops[t]++;
    }
    #initialize(){
        for (let i = 0; i < this.columns; i++{
            this.rainDrops[i] = new Symbol(i, 0, this.fontSize, this.canvasHeight);
        }
    }
    resize(width, height){
        this.canvasWidth = width;
        this.canvasHeight = height;
        this.columns = this.canvasWidth/this.fontSize;
        this.rainDrops = [];
        this.#initialize,
    }
};
setInterval(draw, 30);
window.addEventListener('resize', () => {
    canvas.height = window.innerHeight;
    canvas.width = window.innerWidth;
    effect.resize(canvas.width, canvas.height);
})
